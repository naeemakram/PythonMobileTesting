[pytest]
# 1. Show logs in the terminal live? (Yes/No)
log_cli = true
log_cli_level = INFO

# 2. Format the logs (Time | Level | Message)
log_format = %(asctime)s [%(levelname)s] %(message)s
log_date_format = %Y-%m-%d %H:%M:%S